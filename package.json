{
  "name": "microsoft-communications-portal",
  "version": "1.1.0",
  "description": "Single pane of glass for Microsoft updates (Power Platform, M365, Azure, Message Center, Service Health) with AI-powered impact summarization",
  "license": "MIT",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "build:swa": "node scripts/build-swa.js",
    "test": "node --test scripts/test-auth.js",
    "lint": "eslint ."
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "eslint": "^10.0.0",
    "globals": "^17.0.0",
    "playwright": "^1.52.0"
  }
}
